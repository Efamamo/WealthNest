'use server';

export const signIn = async (data: signInProps) => {
  try {
  } catch (error) {
    console.error(error);
  }
};

export const signUp = async (userData: SignUpParams) => {
  try {
  } catch (error) {
    console.error(error);
  }
};
